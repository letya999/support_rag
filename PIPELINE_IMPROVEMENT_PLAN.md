# 🚀 План улучшения RAG Pipeline

> **Версия:** 2.0  
> **Дата обновления:** 2026-01-05  
> **Статус:** В реализации (Фаза 5)

---

## 📋 Содержание

1. [Текущий статус](#текущий-статус)
2. [Архитектура конфигурации](#архитектура-конфигурации)
3. [Фазы реализации](#фазы-реализации)
4. [Детальные задачи (Остатки)](#детальные-задачи-остатки)
5. [Технические нюансы](#технические-нюансы)
6. [Критерии готовности](#критерии-готовности)

---

## 🔍 Текущий статус

Большинство критических проблем архитектурного уровня решены в рамках Фаз 0-2.

### ✅ Решенные проблемы
| # | Проблема | Решение |
|---|----------|---------|
| 1 | Грязная история диалога | Внедрен `history_filter.py` и `system_phrases.yaml`. |
| 2 | Aggregation без контекста | Нода `aggregate` теперь учитывает N сообщений истории. |
| 4 | Хардкод категорий/интентов | Динамический реестр `intent_registry.py` + скрипт автогенерации. |
| 5 | Multihop обрезает до 1 док. | Параметр `output_docs_count` позволяет передавать массив документов. |
| 6 | Неверный порядок Rerank | Переставлено в `pipeline_config.yaml`: сначала Rerank, потом Multihop. |
| 7 | Нет автогенерации реестра | Скрипт `refresh_intents.py` + Registry Service. |
| 9 | Routing без confidence | Добавлен порог `min_confidence_auto_reply` в настройках ноды. |
| 10| Prompt Routing с пустой историей| Используется `conversation_history` из БД (Postgres). |
| 11| State Machine (простая) | Rules Engine с декларативными правилами в `rules.yaml`. |

### 🔴 Оставшиеся проблемы (Приоритет: Фаза 5)
| # | Проблема | Влияние |
|---|----------|---------|
| 13| Язык ответа | Риск ответа на неверном языке при смешанном контексте. |

---

## 🏗️ Архитектура конфигурации

Система полностью мигрирована на YAML.

### Структура
- `app/services/config_loader/loader.py`: Единая точка доступа к параметрам.
- `app/pipeline/pipeline_config.yaml`: Манифест графа и глобальные настройки.
- `app/nodes/_shared_config/`: Общие ресурсы (`system_phrases.yaml`, `languages.yaml`).

---

## 📅 Фазы реализации

### Фаза 0-2: Фундамент и Исправления ✅ ВЫПОЛНЕНО
- [x] Миграция на YAML (`PyYAML`).
- [x] Централизованный `config_loader`.
- [x] Фильтрация истории (системные фразы).
- [x] Оптимизация порядка нод (Rerank → Multihop).
- [x] Исправление передачи документов в Multihop.

### Фаза 3: Динамический Реестр ✅ ВЫПОЛНЕНО
- [x] **Автогенерация Интентов и Категорий**: Скрипт `scripts/refresh_intents.py`.
- [x] **Registry Service**: `app/nodes/_shared_config/intent_registry.py` - синглтон для работы с реестром.
- [x] **Обновление классификатора**: `SemanticClassificationService` использует динамические списки.
- [x] **API эндпоинты**: `/admin/refresh-intents` и `/admin/intents-registry` для управления.


### Фаза 4: State Machine Rules Engine ✅ ВЫПОЛНЕНО
- [x] **Декларативные правила**: `app/nodes/state_machine/rules.yaml` с приоритетами и действиями.
- [x] **Rules Engine**: `app/nodes/state_machine/rules_engine.py` - загрузка и применение правил.
- [x] **Интеграция с dialog_analysis**: Сигналы (gratitude, frustration, repeated_question) обрабатываются.
- [x] **Умный выбор промпта**: `prompt_routing` использует `state_behavior` для тона и подсказок.
- [x] **Новые промпты**: EMPATHY_MODE и CLARIFY состояния.

### Фаза 5: Аналитика и Польза 📅
- [ ] LLM-агрегация (экспериментально).
- [ ] Детекция языка (на уровне `generation` или `prompt_routing`).
- [ ] Сборка единого конфига для деплоя (`build_config.py`).

---

## 📝 Детальные задачи (Остатки)

### Задача 3: Иерархический Реестр (Категории → Интенты) из БД (Фаза 3)

**Проблема:** Интенты и категории захардкожены в коде (`prompts.py` и `semantic_classifier.py`). Это делает систему негибкой: при добавлении новых документов в базу классификатор не знает о новых темах.

**Решение:** Создание динамического реестра, где первичным элементом является **Категория**, а внутри неё группируются **Интенты**. Все данные извлекаются напрямую из метаданных документов в БД.

**Скрипт:** `scripts/refresh_intents.py`
```python
# Логика:
# 1. Подключение к Postgres (хранилище документов).
# 2. Получение списка уникальных категорий (DISTINCT metadata->>'category').
# 3. Для каждой категории получение списка уникальных интентов (DISTINCT metadata->>'intent').
# 4. Сборка иерархического YAML-файла.
```

**Реестр:** `app/nodes/_shared_config/intents_registry.yaml`
```yaml
# ⚠️ АВТОГЕНЕРИРУЕМЫЙ ФАЙЛ (Priorities: Category > Intent)
_meta:
  generated_at: "2026-01-05T..."
  source_db: "postgres:documents"

# Иерархия: Категория -> Список интентов
categories:
  - name: "Technical Support"          # Взято из БД
    description: "Автоматическое описание..." # Опционально
    intents:                           # Группировка
      - "api_error"
      - "auth_failure"
  
  - name: "Billing"
    intents:
      - "invoice_request"
      - "subscription_cancel"
```

**Использование в классификации:**
1. **SemanticClassificationService** при инициализации загружает `intents_registry.yaml`.
2. В первую очередь выполняется классификация по **Категориям** (верхний уровень).
3. Затем (или параллельно) уточняется **Интент**, но только из списка допустимых для выбранной категории.
4. Если категория из БД «незнакома» сервис-маппингам (нет в `CATEGORY_MAP`), используется её имя как есть (fallback).

---

### Задача 4: State Machine Rules Engine (Фаза 4)

**Файл:** `app/nodes/state_machine/rules.yaml`

Создание системы переходов на основе "сигналов":
- `is_gratitude` (из dialog_analysis) → `RESOLVED`
- `repeated_question` → `CLARIFY` или `ESCALATE`
- `sentiment == extreme_negative` → `ESCALATE`

---

## 🛠️ Технические нюансы

1. **DB Single Source of Truth**: Категории в реестре ТРЕБУЮТ наличия хотя бы одного документа в базе с соответствующей мета-информацией.
2. **Маппинг (Enrichment)**: Поскольку в БД хранятся технические ключи (`billing`), а для семантического поиска нужны описания ("Payment and invoices"), скрипт может подтягивать расширенные описания из отдельного справочника, если они есть.
3. **Безопасность**: Игнорирование пустых (`null`) или `unknown` категорий при генерации реестра.

---

## ✅ Критерии готовности

1. **Для Фазы 3**:
   - Скрипт `refresh_intents.py` успешно читает **базу данных** и строит иерархию Категория -> Интенты.
   - Реестр `intents_registry.yaml` содержит актуальные данные из хранилища.
   - Классификатор корректно работает с новыми категориями, которых не было в коде изначально.

2. **Для Фазы 4**:
   - `state_machine` переключает состояния согласно `rules.yaml`.
   - `prompt_routing` выбирает разные System Prompt для `INITIAL` и `EMPATHY_MODE`.
